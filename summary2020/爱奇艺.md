1:解释一下prototype 的继承是如何实现的

class Parent {

  constructor (props) {

​    }

  }

  class Child {

​      constructor (props) {

​    }

​    }

​    C

2:括号序列由( )[ ]{ }组成，不合法的括号序列由( { ) }，[ } { ]，等等。编程实现一个函数，检查一个括号序列是否是合法的括号序列。

const check = function(str) {

  const map = new Map();

  str

  }

  const count = function(dom) {

​    let result = 0;

​    

​    

​     const dfs = function(parent) {

​       parent.value === 'abc' && 

​       if(parent.children.length > 0) {

​         parent.children.forEach(child => {

​           dfs(child)

​           })

​         } 

​    }

​    dfs(dom)

​    }

  

  

  async await

async函数就是generator函数的语法糖。

async函数，就是将generator函数的*换成async，将yield替换成await。

 语法糖，如何实现

​      父

   子1 子2 子3 



