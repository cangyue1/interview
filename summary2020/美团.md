\1. box-sizing 作用 标准的盒子模型

\2. 垂直居中的 几种方式？  flex transform   margin

\3. flex 使用

\4. rem 作用，做什么 vw, vh 

JS

\1. var let const 区别

   const a = [2]; 

   引用类型 基础类型  

   let a;  判断 a 的类型？

   instanceOf  tyepof 其他方式？

\2. call apply bind 区别  

   类数组 

   Arrry.from 

   apply(this, 数组)

   实现 callSelf 函数

   .callSelf();

\3. const a = { .... };  遍历对象?

   for in 

\4. 封装一个请求？ 用 Promise 封装？

  const request = function() {

​     

  });

 request.get(url, method, params)

  .then().catch().then(() => { })

\5.  vue 生命周期， 父组件 嵌套 子组件

\6. script async defer

B端 



手写call